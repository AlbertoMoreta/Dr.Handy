
namespace DrHandy.Droid.Interfaces {
    public interface IHealthFragment {
        /* Marker Interface. Should implement this interface the header and body fragment
         * of your health module, as well as the health card fragment if exists, and any 
         * other fragment used in your health module (for example tab fragments) 
         */
    }
}
